[project]
name = "rambo-rainer-backend"
version = "1.0.0"
description = "Rambo Rainer Flask-Backend (Regeln, Scoring, Sync, DB)"
requires-python = ">=3.9"
dependencies = [
  "flask-socketio>=5.3.0",
  "python-socketio[asyncio_client]>=5.10.0",
]
